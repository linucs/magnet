= simple_form_for(@board, remote: true) do |f|
  .modal-body
    = f.error_notification

    = hidden_field_tag :in_place, params[:in_place]
    = hidden_field_tag :page, params[:page]
    = f.association :users, collection: current_user.teammates
  .modal-footer
    = f.button :submit

.login-box
  .login-logo
    %b>Mag
    NET
  / /.login-logo
  .login-box-body
    %p.login-box-msg Forgot your password?
    = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      = f.input :email, label: false, wrapper_html: {class: 'has-feedback'} do
        = f.input_field :email, placeholder: 'E-mail', class: 'form-control', autofocus: true
        %span.glyphicon.glyphicon-envelope.form-control-feedback
      .row
        .col-xs-12
          = f.button :submit, 'Send me reset password instructions', class: 'btn-block'
  / /.login-box-body
  .box-footer= render 'devise/shared/links'
/ /.login-box
